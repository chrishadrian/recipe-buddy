import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#8BC34A',
        },
        secondary: {
            main: '#689F38',
        },
        tertiary: {
            main: '#558B2F',
        },
        common: {
            white: '#FFFFFF',
        },
    },
});

export default theme;